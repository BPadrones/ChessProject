#include "myChess.h"

// REQUIREMENTS FOR SELF SATIFACTION
// - I want the game chess to work in a separate library
//   - I want to work chess as series bitboards
// - I want the bot to work as a separate library
// - I want the visualization to work as a separate library
int main() {

  ChessGame game1;
  // initialize game
  //
  //  -[ ] create bitbkards
  // -[ ]
  /*
   while(GameNotdone){
     //Update update Boardstate
     //Visualize Game
     // - [ ] terminal visualization
     // - [ ] take in input in form of a string that represents the board.FEN
   notation
     //take in player input
     // - [ ] take in input as source(A1,alpha,numeric) to destination(A,5)
   }*/
  return 0;
}
